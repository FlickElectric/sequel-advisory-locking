# Sequel::AdvisoryLocking

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'sequel-advisory-locking'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install sequel-advisory-locking
